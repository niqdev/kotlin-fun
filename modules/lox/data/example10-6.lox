fun makeCounter() {
  // closure
  var i = 0;

  // local function
  fun count() {
    i = i + 1;
    print i;
  }

  return count;
}

var counter = makeCounter();
counter(); // "1"
counter(); // "2"
counter();
